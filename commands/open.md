---
description: Open (create) a new room and join it
---

Run `rooms open $ARGUMENTS` and report the result. This creates a room and
auto-joins you to it as the creator. Pass `--topic "..."` to describe what the
room is for, and `--label NAME` to set your display name. Example:
`rooms open standup --topic "morning sync" --label tweed`.
