---
description: Close a room you created (erases it; --archive keeps a copy)
---

Run `rooms close $ARGUMENTS` and report the result. Only the room's creator can
close it. Rooms are ephemeral: closing **erases** the room by default. Pass
`--archive` to keep a verbatim copy (for troubleshooting) instead of erasing.
