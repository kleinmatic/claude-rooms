---
description: Close a room you created (archive, or --erase to delete)
---

Run `rooms close $ARGUMENTS` and report the result. Only the room's creator can
close it. By default the room is archived; pass `--erase` to delete it outright.
